package com.barbet.gauthier.models;

/**
 * Now what is T ?? T is type parameters (also called type variables); delimited
 * by angle brackets (<>), follows the class name. T is just a symbol, like a
 * variable name (can be any name) declared during writing of the class file.
 * Later that T will be substituted with valid Class name during initialization
 * (HashMap<String> map = new HashMap<String>();)
 * 
 * e.g) class name<T1, T2, ..., Tn>
 * 
 * So Class<T> represents a class object of specific class type 'T'.
 * 
 * Assume that your class methods has to work with unknown type parameters like
 * below
 * 
 * @author Administrateur
 *
 */
public class T {

}
